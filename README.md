# Horror Movie Recommender (Deprecated)

This repo has been merged into [movie-night](https://github.com/benjaminlobato/movie-night). The recommender engine (`hybrid_recommender_v4.py`) and all related scripts now live in `movie-night/scripts/`.

The Letterboxd ratings parquet file (`data/letterboxd-ratings/titouann_10m.parquet`) has been moved to `movie-night/data/letterboxd-ratings/` (gitignored due to size).

No further development will happen here.
